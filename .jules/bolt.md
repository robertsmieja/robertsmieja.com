## 2024-03-14 - Removed Unnecessary Browser Availability Check in Layout

**Learning:** The layout component in this Gatsby application was wrapped in a function `isBrowserAvailable()` which checked `typeof window !== "undefined"`. This caused the layout (and therefore all pages) to not be fully server-side rendered or statically generated by Gatsby initially, as `window` is undefined during the Node.js build process. Instead of pre-rendering the full HTML, Gatsby likely rendered an empty fragment or partial layout that only fully mounted on the client-side once hydration occurred and `window` became available. This is an anti-pattern for Gatsby, defeating the main performance benefit of Static Site Generation (SSG).

**Action:** Remove the `isBrowserAvailable` wrapper from the Layout component, allowing Gatsby to fully pre-render the page HTML at build time for faster initial page loads, better SEO, and less client-side work.
